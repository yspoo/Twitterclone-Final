<%= form_for @newtweet do |f| %>

<center>
  <div class="field">
    <%= f.text_field :content, autofocus: true %>
  </div>
  <div class="actions">
    <%= f.submit "Tweet" %>
  </div>
</center>

<% end %>
